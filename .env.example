# note 로그인 세션 쿠키 (httpOnly). `npm run refresh-cookie`로 Chrome에서 자동 추출 가능.
NOTE_SESSION_V5=

# 로컬 markdown이 위치한 디렉터리 (기본: ./content)
NOTE_CONTENT_DIR=./content

# (선택) refresh-cookie가 사용할 Chrome 프로필 이름. 비우면 자동 탐지.
# 예: "Profile 1", "Default"
NOTE_CHROME_PROFILE=
